# Verification Layer Freeze Notice

**Status:** Frozen (Bridge-of-Bridge) · **Issuance Unaffected**

During cross-platform recomputation, identical nominal procedures produced divergent computed results across different environments. The root cause is currently unknown and outside the issuer’s operational boundary.

Therefore:
- This divergence is **not used** as an issuance condition.
- No compatibility adaptation will be performed at the Core layer.
- Recomputation convergence is moved to **Bridge-of-Bridge** for possible future, separately scoped integration work.

AuditVisa issuance, signing, and delivery remain operational and governed by the issuer-side reference implementation.

---

# 验证层冻结说明

**状态：冻结（Bridge 的 Bridge） · 出证不受影响**

在跨平台重算过程中，同一名义标准流程在不同计算环境中产生了不同的计算结果。原因目前不明，且不属于发行侧可控边界。

因此：
- 该差异**不作为**出证条件。
- Core 层不进行兼容性扭曲。
- “重算一致性/兼容性”移入 **Bridge 的 Bridge**，未来如需对接将以独立范围另行推进。

出证、签名与交付链路保持正常运行，以发行侧参考实现为准。
